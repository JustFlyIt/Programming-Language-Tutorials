import random

# Bell curve example
# Provide the mean and the standard deviation
# The larger the standard deviation, the wider the range.
for i in range(20):
    print(random.normalvariate(5, 0.2))
